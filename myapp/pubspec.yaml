name: myapp
description: "Smart Product Recommendation App using Flutter & Firebase"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🔥 Firebase Core
  firebase_core: ^2.27.0

  # 🔐 Firebase Authentication
  firebase_auth: ^4.17.0

  # 📦 Firestore Database (CRUD)
  cloud_firestore: ^4.15.0

  # 🖼 Firebase Storage (Images)
  firebase_storage: ^11.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
